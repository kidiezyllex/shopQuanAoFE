export default {
  plugins: {
    tailwindcss: {}, // Sử dụng plugin Tailwind CSS để xử lý các lớp utility
    autoprefixer: {}, // Tự động thêm tiền tố vendor (prefix) cho CSS để tương thích trình duyệt
  },
}
